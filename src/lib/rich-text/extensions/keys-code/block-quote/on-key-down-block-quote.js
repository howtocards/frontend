// import { onModEnter, onBackspace, onEnter } from "../common/events"
// import { isModEnter, isBackspace, isEnter } from "../constant"

export const onKeyDownBlockQuote = ({ opts, event, change, editor }) => {
  const args = { opts, event, change, editor }
  // @TODO: event handling

  return editor()
}
