// @flow
import styled from "styled-components"

export const MenuContainer = styled.div`
  display: flex;
  flex-direction: column;
  border-radius: 0.3rem;
  box-shadow: 0 0.3rem 1rem 0 rgba(0, 0, 0, 0.32);
  background-color: var(--card);
  color: var(--card-text);
  padding: 0.5rem 0;
`
